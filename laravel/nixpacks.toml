[phases.setup]
nixPkgs = ["...", "php84", "php84Extensions.pdo_mysql", "php84Extensions.mysqli", "php84Extensions.pdo"]

[phases.install]
cmds = ["...", "docker-php-ext-install pdo pdo_mysql || true"]
